# fly.toml app configuration file generated for socialvideodownload on 2026-06-18T08:48:00Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'socialvideodownload'
primary_region = 'fra'

[build]
  dockerfile = 'Dockerfile'

[env]
  CORS_ORIGINS = '*'
  DB_NAME = 'saveflex'
  PORT = '8080'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 1024
